import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

import type { ScanResult } from '../types/scan.types';
import { RemediationPlanList } from './RemediationPlanList';
import { ScannerBoundaryNote } from './ScannerBoundaryNote';

export function RemediationBriefCard({ scan }: { scan: ScanResult }) {
  const narrative = scan.narrative;
  return (
    <Card>
      <CardHeader>
        <CardTitle>Remediation brief</CardTitle>
        <CardDescription>
          {narrative?.ai_generated ? 'Generated by the OpenAI Agents SDK.' : 'Deterministic fallback summary.'}
        </CardDescription>
      </CardHeader>
      <CardContent className="space-y-5">
        <div className="rounded-lg border bg-background/50 p-4">
          <p className="text-sm leading-6 text-muted-foreground">
            {narrative?.executive_summary ?? 'No narrative was returned.'}
          </p>
        </div>
        <RemediationPlanList steps={narrative?.remediation_plan ?? []} />
        <ScannerBoundaryNote />
      </CardContent>
    </Card>
  );
}
